import pg from "pg";import {config} from "../config.js";const {Pool}=pg;
export const pool=new Pool({connectionString:config.databaseUrl,ssl:config.databaseUrl.includes("localhost")?false:{rejectUnauthorized:false}});
export async function initDb(){await pool.query(`CREATE TABLE IF NOT EXISTS users(id BIGINT PRIMARY KEY,username TEXT,first_name TEXT,balance NUMERIC(14,4) NOT NULL DEFAULT 0,joined_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS products(id TEXT PRIMARY KEY,name TEXT NOT NULL,price NUMERIC(14,4) NOT NULL DEFAULT 0,warranty TEXT NOT NULL DEFAULT '',description TEXT NOT NULL DEFAULT '',note TEXT NOT NULL DEFAULT '',delivery_note TEXT NOT NULL DEFAULT '',delivery_template TEXT NOT NULL DEFAULT '',active BOOLEAN NOT NULL DEFAULT TRUE,stock_enabled BOOLEAN NOT NULL DEFAULT TRUE,preorder_enabled BOOLEAN NOT NULL DEFAULT FALSE,client_mail_activation BOOLEAN NOT NULL DEFAULT FALSE,parent_id TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS stock_items(id BIGSERIAL PRIMARY KEY,product_id TEXT NOT NULL REFERENCES products(id) ON DELETE CASCADE,value TEXT NOT NULL,sold BOOLEAN NOT NULL DEFAULT FALSE,sold_order_id TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),UNIQUE(product_id,value));CREATE TABLE IF NOT EXISTS orders(id TEXT PRIMARY KEY,user_id BIGINT NOT NULL,product_id TEXT NOT NULL,product_name TEXT NOT NULL,quantity INTEGER NOT NULL,unit_price NUMERIC(14,4) NOT NULL,total NUMERIC(14,4) NOT NULL,status TEXT NOT NULL DEFAULT 'completed',delivery TEXT NOT NULL DEFAULT '',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS balance_ledger(id BIGSERIAL PRIMARY KEY,user_id BIGINT NOT NULL,amount NUMERIC(14,4) NOT NULL,reason TEXT NOT NULL,admin_id BIGINT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS coupons(code TEXT PRIMARY KEY,percent NUMERIC(6,2) NOT NULL,active BOOLEAN NOT NULL DEFAULT TRUE,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE TABLE IF NOT EXISTS help_items(id TEXT PRIMARY KEY,title TEXT NOT NULL,content TEXT NOT NULL,active BOOLEAN NOT NULL DEFAULT TRUE);CREATE TABLE IF NOT EXISTS settings(key TEXT PRIMARY KEY,value TEXT NOT NULL);`)}
export async function ensureUser(id:number,username?:string,firstName?:string){await pool.query(`INSERT INTO users(id,username,first_name) VALUES($1,$2,$3) ON CONFLICT(id) DO UPDATE SET username=EXCLUDED.username,first_name=EXCLUDED.first_name`,[id,username||"",firstName||""])}
export async function getUser(id:number){return (await pool.query("SELECT * FROM users WHERE id=$1",[id])).rows[0]}
export async function getProducts(){return (await pool.query("SELECT * FROM products WHERE parent_id IS NULL ORDER BY created_at,name")).rows}
export async function getProduct(id:string){return (await pool.query("SELECT * FROM products WHERE id=$1",[id])).rows[0]}
export async function stockCount(id:string){return (await pool.query("SELECT COUNT(*)::int n FROM stock_items WHERE product_id=$1 AND sold=FALSE",[id])).rows[0]?.n||0}